'use client';

import AutomataSimulator from './AutomataSimulator';

export default function TMSimulatorPage() {
  return (
    <div className="w-full h-screen">
      <AutomataSimulator />
    </div>
  );
}