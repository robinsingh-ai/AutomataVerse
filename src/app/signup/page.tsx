import Signup from '../components/signup';

export const metadata = {
  title: 'Sign Up | Automata-Verse',
  description: 'Create a new Automata-Verse account',
};

export default function SignupPage() {
  return <Signup />;
} 