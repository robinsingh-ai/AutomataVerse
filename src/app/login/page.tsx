import Login from '../components/login';

export const metadata = {
  title: 'Login | Automata-Verse',
  description: 'Sign in to your Automata-Verse account',
};

export default function LoginPage() {
  return <Login />;
} 